# 字幕範例

本資料夾提供 SRT 與 VTT 格式範例。

實際使用時，請將字幕檔放到 EverCam 課程資料夾中，與 `media.mp4` 放在同一層，再雙擊 `更新字幕.cmd`。

命名格式：

```text
media.語言代碼.srt
media.語言代碼.vtt
```

若只有正體中文字幕，也可使用 `media.srt` 或 `media.vtt`；未提供明確的 `media.zh-TW.srt/vtt` 時，工具會將其視為正體中文（`zh-TW`）。

範例檔案只用來說明格式，不是實際課程字幕。
